# 本地联调后端地址（umi 只加载 .env / .env.local，不读 .env.development）。
# 2026-09-17：本地后端经 group-center/run.local.sh 跑在 15090，
# /web/open/realtime/** 已可用，前端走全真实模式（MOCK=none）。
GROUP_CENTER_URL="http://localhost:15090"
