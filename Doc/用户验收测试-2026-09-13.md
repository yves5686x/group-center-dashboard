# 用户验收测试记录（第一轮，尚未完成联调）

范围：group-center、group-center-dashboard。功能用例来自浏览器可见页面和用户操作，不根据实现代码推导功能正确性。代码合规另行检查。

## 环境与限制

- 本地未运行前后端服务。前端以 MOCK=none、DISABLE_PROXY=true 在 127.0.0.1:18000 启动。
- 当前浏览器结果仅代表后端不可用时的表现，不代表正常联调通过。404 是本轮环境条件，不能直接作为后端缺陷。
- 现有后端本地启动脚本连接真实数据源；README 说明存在定时群通知。未启动该脚本，未发送通知或修改业务数据。
- 等待可用测试网址/账号，或隔离数据库与消息发送的本地测试环境。

## 已执行的浏览器检查

| 场景 | 实际结果 | 判定 |
| --- | --- | --- |
| 进入首页并通过侧栏跳转 | 可进入 GPU、硬盘、任务查询、统计、高级管理、反馈报告 | 页面入口可达，内容正确性未完成 |
| 后端不可用时打开 GPU 看板 | 显示“没有可用的 GPU 服务器 Request failed with status code 404” | 有错误信息，但将服务异常与无服务器混合表达 |
| 后端不可用时打开硬盘看板 | 显示“没有可用的服务器 Request failed with status code 404” | 同上 |
| 任务查询不填条件点击查询 | 出现“空条件查询确认” | 有确认保护；确认后的错误提示需进一步复核 |
| 统计页切换用户统计 | 显示“数据加载失败 网络错误，请稍后重试”和“重试” | 故障提示及恢复入口可见 |
| 高级管理打开订阅管理 | 提示“获取用户列表时发生错误” | 有错误提示，订阅业务未测 |
| 反馈报告 | 展示客户端版本与环境；后端版本失败有提示 | 页面可用，后端版本未验证 |

## 已确认的代码合规问题

### C-01：ESLint 未覆盖业务源码

- 复现：前端目录运行 `./node_modules/.bin/eslint src --no-cache`。
- 实际：退出码 2，提示所有匹配 src 的文件都被忽略。
- 预期：业务源码被实际检查，并明确输出规则违规或通过。
- 影响：不能将提交钩子未报错当作 TypeScript/React 代码合规通过。

## 合规检查结果

- TypeScript：`tsc --noEmit --pretty false`，退出码 0。
- Stylelint：检查 `src/**/*.less`，退出码 0。
- 后端：`./gradlew compileKotlin compileJava --offline --console=plain`，成功；两个编译任务为 UP-TO-DATE，仅确认当前缓存状态，不等于全新编译。
- 后端构建脚本未发现 detekt、ktlint、checkstyle、spotless 配置。编译成功不能替代这些规范检查。

## 下一轮必测

真实数据展示、筛选与重置、分页与排序、任务详情、时间范围边界、统计口径、GPU/硬盘离线与恢复、订阅增删改、身份权限、刷新/返回、窄屏布局。对有写入的用例使用专用可清理测试数据；消息发送和真实资源变更需明确测试范围。

目前不能宣称前后端已全面测试，也没有将静态代码猜测列为已复现业务 Bug。

## 第二轮补充

- 前端生产构建 `max build` 成功（退出码 0）。
- 后端强制重新执行 `compileKotlin compileJava --offline --rerun-tasks` 成功，两个任务均实际执行。
- 编译器报告 Kotlin API 2.0 弃用及若干冗余转换/安全调用警告；`utils/time/TimePeriod.kt:63` 有 `This cast can never succeed` 警告。这属于合规风险，尚未通过用户操作复现，不作为已确认功能 Bug。
- 后端联调启动被自动审批拒绝，原因是命令涉及现有数据库、真实机器清单和外部 Agent，测试请求尚未明确授权使用这些真实资源。命令未执行，后端未启动。已请求允许仅查询和展示真实资源，禁止自动建表、关闭报告推送。

## 第三轮（2026-09-17，Mock 联调）

环境：`npm run dev`（mock 开启、无代理），Playwright 驱动 Microsoft Edge 无头浏览器逐页访问并交互。本轮全部基于 mock 数据，不代表真实后端行为。截图与驱动脚本存于 /tmp/gcd-e2e/。

### 单页面检查（浏览器可见）

| 场景 | 实际结果 | 判定 |
| --- | --- | --- |
| 首页 | 站点卡片正常；机器/代理监控 4 个接口 404，界面无任何提示 | 发现问题 H-02 |
| GPU看板 | 聚合层 mock 数据完整（含 Agent离线、未配置Agent 机器），任务卡片正常 | 通过 |
| 硬盘看板 | 挂载点进度条、高占用告警正常 | 通过 |
| 任务查询 | 空条件确认弹窗正常，但需点两次「确定」才发起查询 | 发现问题 H-01 |
| 统计信息 | 汇总卡片与分布图正常；控制台有 Select options value 为 null 的警告 | 通过（有警告） |
| 高级管理 | /api/public/users 404，用户列表拉不到（mock 未覆盖） | 环境限制 |
| 反馈报告 | 客户端信息完整，后端版本失败有黄色 Alert 提示 | 通过 |
| Dev 路由（dark/access/table/dev_settings） | 无页面错误 | 通过 |

### 整体耦合检查

- 侧栏导航链 首页→GPU→硬盘→任务查询→统计→高级管理→反馈报告 全部可达，浏览器后退正常。
- GPU 看板用户名过滤（zustand）：设置「石峻昊」后芯片变蓝显示「用户名过滤:石峻昊」并有成功提示，store→芯片→任务卡片链路响应正常。
- 实时数据同源聚合层迁移后的前端路由验证通过：/gpu-dashboard 等路由不再被代理规则劫持。

### 确认的问题

- H-01：空条件查询需点两次「确定」。表单层 TaskQueryForm.tsx 与页面层 TaskQuery/index.tsx 各自做了一次空条件确认，两层叠加。
- H-02：首页机器/代理监控获取失败完全静默（仅控制台报错），用户无法感知监控不可用。
- H-03：任务查询请求失败（404）时界面显示「暂无数据」，无法区分「真没任务」和「服务出错」。
- H-04（轻微）：GPU 看板右侧悬浮的机器快速跳转面板默认展开，遮挡「未配置Agent的机器」筛选芯片；全站存在 antd 6 弃用 API 警告（Space direction、Card bordered、Divider type、Statistic valueStyle、Alert message）。

### 本轮修复（H-01 / H-02 / H-03）

- 删除表单层空条件确认，保留页面层一处确认，空条件查询只需确认一次。
- 任务查询失败时显示红色 Alert 与 toast 明确报错，重置时清除。
- 首页机器/代理监控获取失败时显示黄色 Alert「监控数据获取失败，当前显示的可能是旧数据，将自动重试」，保留旧数据不重置的原有行为。
- `tsc --noEmit` 退出码 0。修复后的浏览器复测见下轮记录。

### 遗留建议

- mock 未覆盖：/web/open/gpu-tasks/query、/api/public/users、/api/machine|proxy/status、/version。补齐后 H-03 类问题可在纯前端环境回归。
- H-04 未修复，待排期。

### 第三轮补充（2026-09-17，按后端仓库补齐 mock）

- 依据后端仓库 /Users/yves/code/web/group-center 的 controller 与 DTO（GpuTaskQueryController、PublicApiController、MachineStatusController、ProxyController、VersionController），新增 mock/groupCenterAPI.ts，覆盖：GPU 任务查询（分页/筛选/排序）、任务详情、用户列表、项目订阅增删查、机器状态与汇总、代理服务器与状态、/version（纯文本）。
- 包装方式按后端实际还原：ClientResponse（isSucceed/result）、proxy 自定义（success/message）、机器状态无包装、/version 纯文本；分页字段 currentPage/totalPages/totalItems。
- 浏览器复测通过：首页机器/代理监控有数据且无告警条；任务查询空条件确认后返回 57 条、按用户筛选 12 条全部为该用户；订阅管理可选 5 名用户并显示已有订阅；反馈报告 GroupCenter Version 显示 1.8.8，无错误提示。tsc --noEmit 退出码 0。
